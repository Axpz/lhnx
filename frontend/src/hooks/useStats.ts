import { useQuery } from '@tanstack/react-query'
import { useCompanyStats } from '@/hooks/useCompanies'
import { useOEMStats } from '@/hooks/useOEM'
import { useProductStats } from '@/hooks/useProducts'

// 统计数据查询 - 聚合所有统计数据
export function useStats() {
}
